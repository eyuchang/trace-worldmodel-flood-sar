name: Delta confirmatory-v7 (superseded; execution disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: >-
          echo "confirmatory-v7 was superseded-before-execution by the committed v9 protocol;
          no seed is executed by this historical workflow"
