name: Delta confirmatory-v6 (superseded; execution disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: >-
          echo "confirmatory-v6 was superseded before execution by the committed v8 protocol;
          no seed is executed by this historical workflow"
